{
  "name": "aus-tender-scraper",
  "version": "1.0.0",
  "description": "Automated scraper for AusTender listings using Playwright",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npx playwright install && npx playwright install-deps"
  },
  "dependencies": {
    "playwright": "^1.43.1"
  }
}
